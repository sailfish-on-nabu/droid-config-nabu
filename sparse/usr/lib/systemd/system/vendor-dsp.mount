[Unit]
Description=Droid mount for /vendor/dsp
Before=local-fs.target systemd-modules-load.service

[Mount]
What=/dev/sde12
Where=/vendor/dsp
Type=ext4
Options=ro,nosuid,nodev,barrier=1
# Options had SELinux context option:  

# Default is 90 which makes mount period too long in case of
# errors so drop it down a notch.
TimeoutSec=10

[Install]
WantedBy=local-fs.target

# From ./out/target/product/nabu/vendor/etc/fstab.qcom :
# /dev/sde12                       /vendor/dsp            ext4    ro,nosuid,nodev,barrier=1                            wait
